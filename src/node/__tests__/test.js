var assert = require('assert');

//before

describe('Deck', function() {
  it('should shuffle cards with a number, startIndex and andIndex', function() {
    assert.equal(-1, [1,2,3].indexOf(4));
  });
  it('should deal cards with a number', function() {
    assert.equal(-1, [1,2,3].indexOf(4));
  });
});
